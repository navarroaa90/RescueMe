<h1> List of Adoptions </h1>

<p><%= link_to 'Post Adoption', 'pets/new'%> </p>

<body>
<ul> 
    <% @pets.each do |pet| %>
        <li>
             <strong> <%= link_to(pet.pet_name, pet_path(pet.id))%> </strong> - <%= pet.breed %>
        </li>
    <% end %>
    </ul>
</body>